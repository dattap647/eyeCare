const useStyle = (theme) => ({
  container: {
    display: "flex",
    alignItem: "center",
    justifyContent: "center",
    // gap: theme.spacing(3),
    // [theme.breakpoints.down("md")]: {
    //   display: "none",
    // },
  },
});
export default useStyle;
